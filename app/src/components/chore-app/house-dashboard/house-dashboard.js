import template from './house-dashboard.html';

export default {
    template,
    controller, 
    bindings: {
        id: '<',
        house: '<', 
        user: '<'
    }
};

function controller() {
    
}
